# Example only. Do not commit real secrets.
# Configure real values in local env and hosting environment settings.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Reserved for later auth milestone wiring.
# AUTH_SECRET="replace-me"
# NEXTAUTH_URL="http://localhost:3000"

# Temporary Task 3 admin guard for backend route smoke testing.
# Comma-separated emails matched against x-platform-admin-email request header.
PLATFORM_ADMIN_EMAILS="admin@example.com,owner@example.com"
